##
# /etc/defaults/distcc
# {{ ansible_managed }}
# DistCC worker (server) config

STARTDISTCC=true
ALLOWEDNETS="{{ distcc_nets }}"
LISTENER="{{ distcc_listen }} --log-level={{ distcc_loglevel }}"
NICE="{{ distcc_nice }}"
ZEROCONF=""
JOBS=""
